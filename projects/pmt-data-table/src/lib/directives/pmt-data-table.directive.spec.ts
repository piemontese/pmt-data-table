import { PmtDataTableDirective } from './pmt-data-table.directive';

describe('PmtDataTableDirective', () => {
  it('should create an instance', () => {
    const directive = new PmtDataTableDirective();
    expect(directive).toBeTruthy();
  });
});
