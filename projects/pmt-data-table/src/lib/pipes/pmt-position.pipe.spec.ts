import { PmtPositionPipe } from './pmt-position.pipe';

describe('PmtPositionPipe', () => {
  it('create an instance', () => {
    const pipe = new PmtPositionPipe();
    expect(pipe).toBeTruthy();
  });
});
